name: docs-page
description: Docs hub lists every tool and CTAs resolve to the right docs sites
tags:
  - smoke
  - full

steps:
  - navigate: "/docs"

  - waitForNetworkIdle:
      timeout: 5000

  - assert:
      visible: "Docs for every tool"

  - assert:
      visible: "Prowl CLI"

  - assert:
      visible: "Prowl Code Review"

assertions:
  # CLI and Code Review docs cards use approved docs.prowl.tools CTA destinations.
  - selectorExists: 'a[href="https://docs.prowl.tools"]:has-text("Prowl CLI")'
  - selectorExists: 'a[href="https://docs.prowl.tools"]:has-text("Prowl Code Review")'
  - selectorExists: "a[href^='https://hub.prowl.tools']"
  - selectorExists: "a[href^='https://infra.prowl.tools']"
