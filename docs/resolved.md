# Prowl Web - Resolved Items

### ~~WEB-001: Feature Agent Capabilities on Landing Page~~
**Resolved**: 2026-02-16 (commit 87e5e86, branch: web-fixes)
**Description**: Added standalone AgentEfficiency section to the marketing landing page positioning Prowl QA as the token-efficient alternative to MCP-based testing. Side-by-side comparison cards show MCP tool-call approach (~15,000 tokens) vs Prowl CLI (~1,000 tokens) with syntax-highlighted code blocks and visual token bars. Four highlight cards cover `--json` structured output, structured exit codes, library API, and zero-overhead discovery. Updated "CI ready" feature card to "CI & agent ready", added agent FAQ entry. CTA links to agent integration docs.

### ~~P1.8-002: Marketing Landing Page (prowlqa.dev)~~
**Resolved**: 2026-02-13 (branch: ui-upgrade)
**Description**: Production-quality marketing landing page for Prowl at prowlqa.dev. Upgraded from scaffold to polished, component-based architecture with 11 sections.

**Architecture**:
- Extracted monolithic `page.tsx` into 11+ components (Nav, Hero, WhyE2E, HowItWorks, CodeExample, Features, Comparison, Install, Faq, FinalCta, Footer)
- Design system with CSS custom properties (`globals.css`) and Tailwind v4 `@theme inline` tokens
- Shared animation utilities (`lib/animations.ts`) and data files (`lib/features-data.ts`, `lib/comparison-data.ts`, `lib/faq-data.ts`)

**Features delivered**:
- Dark/light mode toggle with `useSyncExternalStore`, localStorage persistence, system preference fallback, and FOUC prevention via inline `<head>` script
- Framer Motion animations: scroll-triggered section reveals, stagger effects, terminal typing effect
- Hero: two-column layout with gradient headline, code card (YAML + PASS result bar), mascot overlay, install command bar
- WhyE2E: 3-card grid explaining end-to-end testing value
- HowItWorks: 4-step workflow with IDE-style syntax-highlighted code snippets
- CodeExample: side-by-side YAML hunt + terminal output with hand-coded syntax coloring and typing animation
- Features: 8-card grid with inline SVG icons and staggered scroll reveal
- Comparison table: Prowl vs Playwright vs Maestro across 8 categories (desktop table + mobile cards)
- Install: numbered steps with per-command copy-to-clipboard buttons
- FAQ: native `<details>`/`<summary>` accordion (4 items)
- FinalCta: two-column CTA with mascot image
- Brand assets: Prowl logo in nav, footer, and browser favicon; mascot in hero and CTA sections
- IDE-style syntax highlighting across Hero, HowItWorks, and CodeExample code blocks (purple keys, cyan actions, green strings, yellow keywords)

**Quality**:
- Responsive: mobile hamburger menu, responsive grids, tested at 375px/768px/1280px
- Accessibility: ARIA labels on icon-only buttons, `aria-hidden` on decorative SVGs, semantic HTML, focus-visible rings, table `<caption>` and `scope` attributes, `<nav aria-label>` on footer
- Security: `rel="noopener noreferrer"` and `target="_blank"` on all external links, clipboard error handling
- Hydration safety: consistent server/client DOM rendering, `suppressHydrationWarning` on `<html>`, `useSyncExternalStore` for theme state
- Build: `npm run lint` and `npm run build` passing with zero errors

### ~~WEB-002: Update Marketing Site with Shipped CLI Features~~
**Resolved**: 2026-02-18 (commit 8966009, branch: web-fixes)
**Description**: Updated the marketing site to reflect all CLI features shipped since launch. SEO metadata updated to mention visual regression, AI generation, and 26 step types. Hero description surfaces visual regression and AI generation. Features section replaced 4 cards (watch mode, credential redaction, zero config, old CI card) with visual regression, network mocking, conditional logic, and updated CI & agent ready; step count corrected 16→26. Agent Efficiency section overhauled to showcase the analyze→generate→run pipeline with new code snippet, updated bullets, and pipeline-focused highlight cards. Three new FAQ items added (visual regression, AI generation, network mocking) and existing agent FAQ answer rewritten to mention analyze and generate commands.

### ~~WEB-003: Fix CLI Binary Name Inconsistencies~~
**Resolved**: 2026-02-18 (commit 8966009, branch: web-fixes)
**Description**: Fixed all CLI binary name inconsistencies across the landing page. HowItWorks steps 1 and 3: `prowl` → `prowlqa`. Install section: `prowlai` → `prowlqa` (step 1), `prowl` → `prowlqa` (steps 3 and 4). Grep verified no remaining bare `prowl` or `prowlai` binary references in code examples.

### ~~WEB-004: Remove Competitor Comparison Table~~
**Resolved**: 2026-02-18 (commit 8966009, branch: web-fixes)
**Description**: Deleted `src/components/Comparison.tsx` and `src/lib/comparison-data.ts` — dead code not imported in `page.tsx`. No remaining imports or references. Focus shifted to own product value proposition rather than competitor positioning.

### ~~DOCS-003: Cross-linking to Community Hub and Landing Page~~
**Resolved**: 2026-02-18 (commit ef934b4, branch: hot-fixes)
**Description**: Updated all community hub links from GitHub repo URL to live site at `hub.prowlqa.dev`. Added "Hub" navbar link. Updated footer link and all 6 inline links in `agents.mdx`. Landing page links (`prowlqa.dev`) were already in place in both navbar and footer.

### ~~P4-004: `copyText` Step Type~~
**Resolved**: 2026-03-21 (branch: feature/copytext-random-download)
**Description**: Added `copyText: { selector, as }` step type that extracts `textContent` from an element and stores it as a runtime variable for `{{VAR}}` interpolation in subsequent steps. Includes forbidden selector check, null text content error handling, schema validation, interpolation support, and unit tests.

### ~~P4-009: `waitForDownload` Step Type~~
**Resolved**: 2026-03-21 (branch: feature/copytext-random-download)
**Description**: Added `waitForDownload` step type that captures file downloads via `page.waitForEvent('download')`. Supports bare form (`waitForDownload:` / null), optional `filename` assertion against `download.suggestedFilename()`, and configurable `timeout` (default 30s). Downloaded files saved to run artifacts directory. Schema validation, interpolation support, and unit tests included.

### ~~P6-004: Random Data Generators~~
**Resolved**: 2026-03-21 (branch: feature/copytext-random-download)
**Description**: Added built-in `{{RANDOM_*}}` variables generated once per hunt run: `RANDOM_EMAIL` (prowl_<hex>@test.com), `RANDOM_NAME` (random first+last), `RANDOM_NUMBER` (4-digit integer), `RANDOM_UUID` (v4 UUID), `RANDOM_TEXT` (8-char alphanumeric). Generated at lowest priority so env vars and hunt vars can override. Consistent within a single hunt run.

### ~~PQW-002: Add MCP / agent-native messaging to the marketing site (shipped in prowlqa 0.1.1)~~
**Resolved**: 2026-05-29 (commit e6dbfd2, branch: site-updates)
**Description**: Surfaced the agent-native MCP features shipped in prowlqa 0.1.1 on the marketing site. Added a dedicated "Native MCP server" panel to the AgentEfficiency section covering `prowlqa mcp`, the four exposed tools (`list_hunts`, `run_hunt`, `run_suite`, `list_projects`), MCP client config, no-shell-access, guardrail enforcement, automated bug-logging, and the multi-project registry. Updated the section intro to note the CLI or native MCP server, the "CI & agent ready" Features card to mention the MCP server, and reworked the AI-agents FAQ answer to present both CLI and MCP paths plus a dedicated "Does Prowl QA have an MCP server?" entry. Competitor comparison (AC #3) skipped — the only comparison table is unused/not rendered.

### ~~PQW-001: Add Links to Documentation Site and Community Hub~~
**Resolved**: 2026-06-01 (commit ee1117c, branch: close-out)
**Description**: Unblocked once the hub went live at hub.prowlqa.dev (verified HTTP 200). Updated the footer "Community Hub" link from the prowl-hub GitHub repo to hub.prowlqa.dev. Added a dedicated Community section to the landing page (after Install) highlighting the hub — ready-made hunt templates, plain copy-paste YAML, and contribute-back — with CTAs to the Hub and the prowl-hub repo. Added a "Hub" link to the nav (desktop + mobile) for prominence. Docs links were already in place (nav, hero, footer); no changes needed there.

### ~~Rebrand: Prowl QA → Prowl + suite hub (marketing site)~~
**Resolved**: 2026-06-05 (branch: rebrand-to-prowl-tools)
**Description**: Final repo of the suite rebrand. Mechanical rename across the site: ProwlQA/Prowl QA/ProwlAI→Prowl, `prowlqa` command→`prowl`, install/package→`prowl-tools` (`npm install -g prowl-tools`, `brew tap prowl-tools/tap && brew install prowl`), `prowlqa.dev`→`prowl.tools` (+ docs/hub subdomains, RSS author email), `github.com/Prowl-qa`→`prowl-tools`, and `metadata`/OG/`sitemap`/`robots`/blog-feed → `prowl.tools`. Footer now carries a "Brought to you by Genkei Labs" attribution (linking genkeilabs.com) and the copyright holder is Genkei Labs. Restructured toward a suite hub: hero still leads with the CLI, plus a new `Suite` showcase section with cards for Prowl Hub (hub.prowl.tools), Prowl Infra (infra.prowl.tools), and Prowl Code Review (coming soon). Preserved: `@prowlqa` X handle/creator, the local `Prowl-QA` assets path, and the `introducing-prowl-qa-blog` slug (URL stability). lint + build green. Note: subdomain links (hub./infra.) and the genkeilabs.com link go live with the DNS cutover.

### ~~PQW-017: Install and initialize the Prowl CLI (dogfooding)~~
**Resolved**: 2026-08-05 (commit 8b9f442, branch: prowl-cli-setup)
**Description**: Prowl CLI installed for dogfooding on the marketing site: `prowl-tools@0.1.3` pinned as a devDependency (reproducible in CI, not a global install), `.prowl/` scaffolded via `prowl init` with config targeting `http://localhost:3002` and a starter `hello` hunt, `npm run prowl:install-browsers` added to provision Playwright Chromium on demand, and run artifacts (`.prowl/runs/`, `history.json`) gitignored while config and hunts stay committed. Verified end-to-end: `npx prowl run hello` passes 2/2 steps against the production build. Note: the CLI is repo-local — invoke as `npx prowl` (no global command). Newer CLI versions ship a single starter hunt, so no starter-hunt pruning was needed.

### ~~PQW-018: Author hunts covering the marketing site~~
**Resolved**: 2026-08-06 (commit 06bff10, branch: prowl-hunts)
**Description**: Authored a 10-hunt suite exercising every user-facing surface of prowl.tools: homepage hero, desktop nav (Products dropdown verified opening via hover), mobile hamburger nav, `/cli`, `/code-review`, and `/docs` pages with heading and CTA checks (docs.prowl.tools / product satellite links / GitHub), blog index + `?tag=` filtering + post page, newsletter form presence, dark/light theme toggle (hydration-aware, works from either starting theme), and footer links. Hunts tagged `smoke` (4-hunt fast set, ~3s), `full`, and `mobile` (run with `--viewport mobile`) for CI filtering via `--include-tags`; starter `hello` hunt superseded and removed. All 10 verified passing against the production build.

### ~~PQW-021: Add page-specific Twitter metadata for the blog index~~
**Resolved**: 2026-08-07 (commit 6b97f07, branch: twitter-updates)
**Description**: Added a `twitter` block to the `/blog` metadata in `src/app/blog/page.tsx` so the blog index emits its own Twitter card (title `Blog - Prowl`, blog description, `card: "summary"`, `creator: "@prowltools"`) instead of inheriting the root layout's homepage Twitter title/description. lint + build green.

### ~~PQW-022: Point X/Twitter links and metadata at @prowltools~~
**Resolved**: 2026-08-07 (commit 65aabb1, branch: twitter-updates)
**Description**: Repointed every X/Twitter reference from the unowned `@prowl` handle to the live `@prowltools` account: `twitter.creator` in `src/app/layout.tsx`, `src/app/cli/page.tsx`, `src/app/docs/page.tsx`, and `src/app/code-review/page.tsx`; the footer social link in `src/components/Footer.tsx` (`https://x.com/prowltools`); and the matching footer hunt assertion in `.prowl/hunts/footer.yml`. The `@prowl-review` PR-bot name was deliberately left untouched. lint + build green.

### ~~PQW-019: CI workflow running lint, build, and Prowl hunts~~
**Resolved**: 2026-08-07 (commit 8dbf728, branch: ci-workflow)
**Description**: Added `.github/workflows/ci.yml` running on pull requests and pushes to `main`: `npm ci`, `npm run lint`, `npm run build`, then serves the production build (`npm run start`, port 3002) and polls it with a `curl` retry loop until ready before running the Prowl hunt suite. Uses the dedicated `prowl ci` command against `http://localhost:3002` in two passes — desktop hunts via `--include-tags full`, and the `mobile`-tagged hunt via `--include-tags mobile --viewport mobile` (matching how the PQW-018 suite is authored). Chromium is provisioned with `playwright install --with-deps chromium`; the npm cache (via `actions/setup-node`) and `~/.cache/ms-playwright` (keyed on the installed Playwright version, 1.62.1) are cached; and `.prowl/runs/` plus the server log upload as artifacts on failure. Validated locally: actionlint clean, and a full dry-run of build → start → poll → both `prowl ci` passes green (9 desktop hunts + 1 mobile hunt), with `prowl ci` confirmed to exit 0 on pass / 1 on failure for CI gating. The follow-up PQW-020 (branch protection / deploy gating) remains open.

### ~~PQW-020: Gate production deploys on CI passing~~
**Resolved**: 2026-08-08 (won't do — owner decision)
**Description**: Declined after PQW-019 shipped. Rationale: single-developer repo, and branch protection on `main` would block the standing docs-only direct-push workflow (QA triage, backlog updates). CI (PQW-019) runs on every PR and push to `main`, so red builds are still surfaced immediately — enforcement is left to the owner's discretion. Revisit if the repo gains contributors or if Vercel-side gating (ignored-build-step keyed on CI status) becomes worthwhile.

### ~~PQW-005: Render page content visible without JS (opacity-0 animation / LCP fix)~~
**Resolved**: 2026-08-08 (commit c8c76ec, branch: render-page-content)
**Description**: Marketing sections previously shipped their SSR HTML behind motion/react's inline `opacity:0` `hidden` variant, so content — including the hero headline, the LCP element — was invisible until hydration and hidden entirely for no-JS visitors and crawlers. Added a shared `src/lib/reveal.ts` that renders content visible by default and treats the entrance animation as a progressive enhancement: `revealVisible` (`{ initial: 'visible', animate: 'visible' }`) keeps above-the-fold heroes visible on first paint with no re-hide (correct for LCP; the trade-off is heroes no longer entrance-animate on first load), and `useScrollReveal` gates the `hidden` → `visible` scroll entrance on hydration via a shared one-shot hydration store (no setState-in-effect) that also short-circuits to visible under `prefers-reduced-motion`. Post-hydration scroll reveal props include a stable remount key so Motion applies the hidden initial state on the enhanced mount; standalone `/docs` content stays on the visible hero path. Because below-the-fold sections are off-screen when hydration runs, gating causes no flash and nothing is left permanently hidden. Applied across `Hero`, `SuiteHero`, `CodeReview`, `SectionReveal`, `TypingEffect`, and every `whileInView` section (Features, WhyProwl, DocsHub, WhyE2E, Community, ProductShowcase, AgentEfficiency). Verified: SSR HTML ships zero inline `opacity:0` on content across `/`, `/cli`, `/code-review`, `/docs`, and `/blog` (curl + grep); focused unit coverage covers visible, reduced-motion, post-hydration, and hydration-store behavior; full and mobile hunt suites pass; lint and build green.

### ~~PQW-004: Add Open Graph / Twitter social preview images~~
**Resolved**: 2026-08-08 (commit 5de2207, branch: graph-blog)
**Description**: Added 1200×630 social preview cards via `next/og` `ImageResponse`; because the handlers use no request-time APIs, uncached data, or dynamic configuration, Next statically optimizes and caches the images by default. A shared helper (`src/lib/og-image.tsx`) renders a dark, brand-coloured card — Prowl mascot logo (embedded as a base64 data URI so the build stays offline; the raccoon `public/static/img/prowl-logo.png` reused rather than reaching outside the repo), "Prowl" wordmark, a cyan section eyebrow, a one-line headline, a cyan→green gradient accent bar, and the `prowl.tools` domain. Wired through the `opengraph-image` file convention at the root (`src/app/opengraph-image.tsx`) with title-parameterized per-section variants for `/cli`, `/code-review`, and `/docs`. Each route emits both `og:image` and `twitter:image` (1200×630) — Next fills `twitter:image` from the convention automatically, so no separate `twitter-image` files were needed. Switched `twitter.card` from `summary` to `summary_large_image` on the root layout and the `/cli`, `/code-review`, `/docs`, and `/blog` pages so the large card renders. The root file-convention image applies to descendant routes and more-specific images take precedence; `/blog` still references the site-wide card (`/opengraph-image`) explicitly because its own shallow-merged metadata object replaces the parent `openGraph` object. Verified in rendered HTML via curl across `/`, `/cli`, `/code-review`, `/docs`, `/blog`, and a blog post (correct `og:image`/`twitter:image`/`twitter:card` per page); each `opengraph-image` route returns HTTP 200 `image/png`; full and mobile hunt suites, lint, and build all green.

### ~~PQW-008: Wire blog post `image` frontmatter into metadata~~
**Resolved**: 2026-08-08 (commit 27d9815, branch: graph-blog)
**Description**: `generateMetadata` in `src/app/blog/[slug]/page.tsx` parsed the `image` frontmatter via `src/lib/blog.ts` but never used it, so posts had no preview image. It now sets `openGraph.images` and `twitter.images` from `post.image` (resolved against `metadataBase`), falling back to the site-wide PQW-004 card (`/opengraph-image`) when a post declares none — set explicitly because blog posts define their own shallow-merged metadata objects, while more-specific post images still take precedence. Verified in rendered HTML: a post with an `image` field emits that image for both `og:image` and `twitter:image` (temporary local frontmatter, not committed — no post image asset exists in the repo yet), and the existing imageless post falls back to the site-wide card. Lint and build green; full and mobile hunt suites pass.

### ~~PQW-006: Make the nav Products dropdown keyboard/screen-reader accessible~~
**Resolved**: 2026-08-09 (commit fbd0efa, branch: nav-ally-docs-h1)
**Description**: The desktop Products dropdown in `src/components/Nav.tsx` was CSS-hover/`group-focus-within` only — the trigger had no `aria-expanded`/`aria-controls`, no click or keyboard handler, no Escape-to-close, and defaulted to `type="submit"`, failing WCAG 2.1 SC 4.1.2 (Name, Role, Value) and 1.4.13 (Content on Hover or Focus). Converted it to a JS-controlled disclosure following the WAI-ARIA APG **disclosure** pattern (deliberately not the full ARIA `menu` widget): managed open state, trigger is `type="button"` with `aria-expanded` + `aria-controls` pointing at the menu region, toggles on click/Enter/Space (native button activation), and is dismissible via Escape (returning focus to the trigger), outside pointer press, and focus leaving the container — it also closes when a menu item is selected. Visibility is driven by React state so the menu links are only in the tab order while open, while hover-to-open is preserved for mouse users (no pointer-UX regression). Open/close decision logic is extracted to a DOM-free `src/lib/disclosure.ts` (`disclosureReducer`, `isDismissKey`, `shouldCloseOnFocusOut`) with 5 `node:test` unit tests. The `nav-desktop` hunt was updated in lockstep (the old `aria-haspopup`/`.group` selectors were removed) and extended to assert the disclosure contract: collapsed on load, `aria-expanded` flips true on hover-open, and clicking the trigger toggles it back to false. Verified: lint + build green; `npm test` 15/15; full hunt suite green (nav-desktop 13/13) and mobile hunt suite green (nav-mobile 7/7); homepage still exposes exactly one `<h1>` and no `aria-haspopup`.

### ~~PQW-007: Add an `<h1>` to the /docs page~~
**Resolved**: 2026-08-09 (commit 4ac7ddf, branch: nav-ally-docs-h1)
**Description**: `/docs` rendered `DocsHub` (`standalone`) with its top heading "Docs for every tool" as an `<h2>`, so the page had no `<h1>` — a broken heading hierarchy for assistive tech and a weak SEO signal. `DocsHub` now renders that heading as an `<h1>` when `standalone` and keeps it an `<h2>` when embedded on the homepage, where the hero (`SuiteHero`) owns the page's single `<h1>` (verified in code before the change). Verified in SSR HTML via curl: `/docs` has exactly one `<h1>` ("Docs for every tool", and zero `<h2>` now), and `/` still has exactly one `<h1>` (the hero) with `DocsHub` remaining an `<h2>` there — no duplicate top-level heading. The `docs-page` and `homepage` hunts still pass unchanged (heading text preserved). Lint + build green; `npm test` 15/15.

### ~~PQW-009: RSS feed improvements (autodiscovery, escaping, sitemap)~~
**Resolved**: 2026-08-09 (commit e52c59c, branch: rss-urls)
**Description**: Hardened the blog RSS feed on four fronts. (1) **Autodiscovery**: the feed had no `<link rel="alternate" type="application/rss+xml">` anywhere; added a shared `rssAlternateTypes` descriptor and emit it site-wide via `alternates.types` on the root layout (the conventional placement for a single feed), re-included on pages that set their own canonical because Next shallow-merges the `alternates` field. (2) **Escaping**: `<link>`/`<guid>`/`<atom:link>` URLs were interpolated raw; the feed builder now XML-escapes them via the shared `escapeXml` helper (harmless with today's slugs, correct going forward). (3) **`lastBuildDate`**: the statically generated route froze the timestamp at build time; made that explicit with `export const dynamic = "force-static"` and a documenting comment (the site redeploys on content changes) rather than making the route dynamic, and the build date is now injected into the pure builder. (4) **Sitemap**: `/blog/feed.xml` is listed in `src/app/sitemap.ts` at low priority. The feed logic was extracted into a pure, unit-tested `src/lib/rss.ts` (`escapeXml`, `buildBlogFeed`, `rssAlternateTypes`) with 8 `node:test` cases. Verified: `/blog/feed.xml` returns HTTP 200 `application/xml`, parses cleanly (python `xml.etree`), and shows escaped URLs; the autodiscovery `<link>` appears on `/`, `/blog`, `?tag=` views, and blog posts but not the untouched product pages (confirming shallow-merge, no leak). Lint + build green; `npm test` 26/26; full and mobile hunt suites green.

### ~~PQW-010: Add canonical URLs to /, /blog, and blog posts~~
**Resolved**: 2026-08-09 (commit 0dea434, branch: rss-urls)
**Description**: Product/docs pages already set `alternates.canonical`, but `/`, `/blog`, and `/blog/[slug]` did not. Added `canonical` following the existing pattern (relative paths resolved against `metadataBase`): the homepage canonical (`/`) lives in `src/app/page.tsx` — deliberately not the root layout, since a layout-level canonical is inherited by every child route and would stamp `/` onto unrelated pages; `/blog` canonicalizes to `/blog`, which also collapses `?tag=` filter URLs onto `/blog` and removes the duplicate-content indexing risk; and each post gets a per-post canonical to its own URL via `createBlogPostMetadata`. Each canonical re-includes `rssAlternateTypes` so RSS autodiscovery survives Next's shallow `alternates` merge. Verified in rendered HTML via curl: `/` → `https://prowl.tools`, `/blog` and `/blog?tag=announcements` → `https://prowl.tools/blog`, the post → its own URL, and `/cli`/`/docs` canonicals unchanged with the homepage canonical not leaking onto any page. Lint + build green; `npm test` 26/26; full and mobile hunt suites green.

### ~~PQW-011: Route the FinalCta CTA to an approved docs destination~~
**Resolved**: 2026-08-10 (commit 81d4e3b, branch: medium-tier-cleanup)
**Description**: The FinalCta "How it Works" CTA now points to the approved external docs destination `https://docs.prowl.tools` and uses a native `<a>` with `target="_blank"`/`rel="noopener noreferrer"`, preserving the existing button text and styling while avoiding an unapproved internal CTA target. Verified `https://docs.prowl.tools` returns HTTP 200; lint + build green; `npm test` 26/26.

### ~~PQW-012: Newsletter form cleanup~~
**Resolved**: 2026-08-10 (commit 9f3c499, branch: medium-tier-cleanup)
**Description**: Re-scoped (owner decision): there was no live newsletter — the Buttondown embed action (`https://buttondown.com/api/emails/embed-subscribe/prowl`) pointed at an account we do not own, so the post-footer form silently misdirected/lost real subscriber emails. Removed the newsletter CTA block (heading, copy, and `<form>`) from `src/components/blog/PostFooter.tsx`, keeping the "Back to all posts" link intact. Updated the `blog` hunt in lockstep — the newsletter `scrollTo`/`visible`/`selectorExists` steps now assert the post footer's back-to-blog link (`footer a[href='/blog']`) instead. Restoring a newsletter once a real plan exists is tracked by PQW-023 (restore the block from git history). Verified in generated build output: the rendered blog post has zero `buttondown` strings and no `<form>` element, and the footer-scoped back-to-blog link survives. Lint + build green; `npm test` 26/26.

### ~~PQW-013: Decide canonical product naming on the site~~
**Resolved**: 2026-08-10 (commit 209c019, branch: medium-tier-cleanup)
**Description**: Owner decisions recorded: the code-review product **keeps** its current site name "Prowl Code Review" — no change — while the infra product is renamed "Prowl Infra" → **"Prowl Infra Hub"** to match the live satellite site's own title. Updated `name` in `src/lib/products.ts` (the suite source of truth), which propagates to the nav Products dropdown, footer, and homepage showcase. Satellite URLs (`infra.prowl.tools`) and the `docsLabel` were left untouched, and no string appends "Hub" so there is no "Hub Hub" doubling. No hunt asserts the "Prowl Infra" text (the nav/docs hunts match by `infra.prowl.tools` href), so none needed updating. Verified via curl: `/` renders "Prowl Infra Hub" across nav/showcase/footer with zero "Hub Hub" occurrences. Lint + build green; `npm test` 26/26; full and mobile hunt suites green.

### ~~PQW-014: Code hygiene sweep (dead exports, button types, React keys)~~
**Resolved**: 2026-08-11 (branch: hygiene-sweep)
**Description**: Audited each sub-item against current code (the spec pre-dated PQW-005/PQW-006 refactors) before touching anything. (1) **Dead code removed**: unused exports `getAllTags` (`src/lib/blog.ts`), `fadeIn`/`scaleIn` (`src/lib/animations.ts`), and the unreachable `'exit'` icon case in `src/components/AgentEfficiency.tsx` — grep across `src/` and `tests/` confirmed zero consumers, and `fadeUp`/`staggerContainer` (still used by 11 components) were left in place. (2) **`type="button"` added** to the remaining non-submit buttons: the Nav mobile hamburger (`src/components/Nav.tsx`), `ui/ThemeToggle.tsx`, and both Install copy buttons (`CopyButton`, `CopyAllButton` in `src/components/Install.tsx`). *Already-fixed (no change needed):* the Nav desktop Products trigger already carried `type="button"` from PQW-006's disclosure rebuild. (3) **Stable React keys**: `Install` steps now key on `step.command` (unique) and `TypingEffect` lines on `line.text` (unique in its sole consumer, `CodeExample`, which passes one blank line); `CodeExample`'s YAML rows repeat verbatim (`- fill` with empty value appears twice, so no field is unique on its own) — kept unique with `${line.key}-${i}`, the content field plus its position in the static, never-reordered list. **Rider (folded in):** fixed two pre-existing TypeScript errors in `tests/og-image-content.test.ts` (TS2322 `'unknown'` not assignable to `'string'`, TS18048 `'style'` possibly undefined) that the `next build` gate does not catch — narrowed the optional-chained `backgroundImage` with `assert.ok` (whose `asserts value` signature narrows it to `string`) instead of the non-narrowing `assert.equal`, no cast or `@ts-ignore`. Verified: `npm run lint` clean, `npm run build` green, `npm test` 26/26, `npx tsc --noEmit` clean; full hunt suite green (9 passed / nav-mobile skipped, incl. theme-toggle exercising the touched button) and mobile hunt suite green (nav-mobile 7/7 exercising the touched hamburger).

### ~~PQW-015: Guard against duplicate `<h1>` in MDX blog posts~~
**Resolved**: 2026-08-11 (commit 1e372c0, branch: mdx-h1-guard)
**Description**: `PostHeader` already renders the post title as the page's single `<h1>`, so allowing a post body markdown `#`, setext h1 underline, or raw `<h1>` would emit a second `<h1>` and break the document outline / accessibility. The blog loader now rejects body-level h1 content before render, keeping the authoring convention executable: blog bodies cannot define their own top-level heading, sections conventionally begin at `##`, and deeper headings or intro text remain valid. The MDX component map no longer silently remaps `#` to `<h2>`, preserving normal heading semantics while preventing invalid content from shipping. No existing post used `#` (the current post starts at `##`), so rendered output is unchanged. Locked in with focused `assertNoBodyH1` unit tests plus `blog` hunt assertions that `.prose-prowl h1` and `:nth-match(h1, 2)` do not exist on the rendered post.

### ~~#64: Convert prowl-review to the keyless Codex provider on the self-hosted runner~~
**Resolved**: 2026-08-25 (branch: prowl-review-codex) — non-row infra item (#64 rollout; no matching backlog row)
**Description**: Moved this repo's dogfooded prowl-review off the API-key Claude + Gemini ensemble onto the keyless Codex (ChatGPT subscription) provider running on the self-hosted Mac mini runner (labels `self-hosted, macOS, prowl-review`), so per-review marginal cost is $0.00 and no provider key is stored in GitHub — matching the prowl-code-review repo's own dogfood. **`.prowl-review.yml`**: `provider: codex`, `model: gpt-5.5`, `codex.effort: low`; the `ensemble:` block is retained but commented out with a note (re-enable + restore `PROWL_AI_KEY_ANTHROPIC`/`PROWL_AI_KEY_GEMINI` secrets for an API-key/public-runner fallback); `prDescription`, `issueValidation`, `checkRun` (failOn critical), and `resilience.failback` are unchanged. **`prowl-review.yml`**: the `workflow_run`→CI chain and the hosted `resolve`/`report-unreviewable` jobs are preserved (so fork PRs still get a branded status row without touching the runner); the `review` job moves to `runs-on: [self-hosted, macOS, prowl-review]` behind a mandatory same-repo fork gate (`head_repo == github.repository`, required on this public repo), a non-cancelling per-PR Codex concurrency group (`prowl-review-codex-<repo>-<pr>`), and `timeout-minutes: 30`; every `ai-key-*` input and `PROWL_AI_KEY_*` secret reference is removed in favor of `ai-provider: codex`, while the branded prowl-review[bot] App-token minting (gated on `PROWL_APP_ID`/`PROWL_APP_PRIVATE_KEY`) and the base-branch `config-path`/`guidelines-path` handoff are kept. **`prowl-review-command.yml`**: split from a single ubuntu job into a hosted `resolve` job (trust/fork verdict, no secrets) + a self-hosted `command` job with the same fork gate, Codex concurrency group, timeout, and keyless `ai-provider: codex`; both comment triggers (`issue_comment`, `pull_request_review_comment`) and the branded App identity are preserved. The action `uses:` ref is pinned to reviewed Codex-capable commit `4e60b282f3837b3f09b2a9d0c74f19eef2804c10` in both workflows until a release tag includes the codex provider. Added `.github/actionlint.yaml` registering the `prowl-review` runner label and `tests/workflows.test.ts` coverage that executes the inline resolve scripts with fake GitHub API responses for same-repo, fork, incomplete metadata, malformed output, run-block extraction, PR-number expression replacement, API-failure, ambiguous-match, and stale-head cases. `ci.yml` is unchanged (the chain stays intact). Owner follow-up: delete the now-unused `PROWL_AI_KEY_ANTHROPIC`/`PROWL_AI_KEY_GEMINI` secrets from this repo. Validation after review hardening: actionlint clean on both changed workflows + `ci.yml`; `eslint` clean on tracked source; `npm test` 54/54; `npm run build` green.
